# CRM-spring-mvc-hibernate
simple crm using spring mvc and hibernate


**important**

the directories "lib" , "resources" , "view" , must be put inside a directory named "WEB-INF" , and this , inside a directory named "WebContent"
